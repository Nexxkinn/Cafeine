<Page
    x:Class="Cafeine.TorrentManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cafeine"
    xmlns:m="using:Cafeine.Model"
    xmlns:torrent="using:Cafeine.Design.RemoteTorrent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    DataContext="{Binding TorrentManager, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ListViewItemPresenterCheckMode x:Key="GridViewItemCheckMode">Inline</ListViewItemPresenterCheckMode>
            <m:DirectoryClicked x:Key="DConv" />
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GridView x:Name="List"
                  Margin="17,8,0,0"
                  Foreground="{x:Null}"
                  IsItemClickEnabled="True"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  animations:ReorderGridAnimation.Duration="300"
                  ItemsSource="{x:Bind Vm.list,Mode=OneWay}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="torrent:TorrentModel" >
                    <Grid Width="328" Height="52">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="0">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{ThemeResource SystemListLowColor}"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" FontSize="20" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock Margin="8,0" 
                       Text="{x:Bind name}"
                       FontSize="13.5"
                       Grid.Row="1"
                       Grid.Column="1"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="Wrap"
                       Foreground="#FF333333"
                       MaxLines="2"
                       VerticalAlignment="Center"
                       FontFamily="ms-appx:/Assets/Fonts/Montserrat/Montserrat-SemiBold.ttf#Montserrat"
                       CharacterSpacing="16"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="0,4"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="FocusVisualMargin" Value="0" />
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaxWidth="350" HorizontalAlignment="Left"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <!--<interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="ItemClick">
                    <core:InvokeCommandAction Command="{x:Bind Vm.DirectoryItemClick}"
                                              InputConverter="{StaticResource DConv}" />
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>-->
        </GridView>
        <StackPanel Visibility="{x:Bind Vm.IsErrorVisualVisible, Mode=OneWay}"
                    Orientation="Vertical"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Center"
                    Opacity="0.7">
            <TextBlock Text="Hmm..."
                       FontSize="32"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontFamily="ms-appx:/Assets/Fonts/Montserrat/Montserrat-SemiBold.ttf#Montserrat SemiBold" FontWeight="SemiBold" />
            
            <TextBlock Text="An error" HorizontalAlignment="Center"/>
            <TextBlock Text="{x:Bind Vm.ErrorValue,Mode=OneWay}" HorizontalAlignment="Center"/>
            <TextBlock Text="Got called out instead..." Margin="0,0,0,24" HorizontalAlignment="Center"/>
            <TextBlock Text="0"
                       Canvas.ZIndex="0"
                       Grid.Row="0" Grid.Column="0"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       FontFamily="ms-appx:/Assets/Fonts/Cafeine_font.ttf#Untitled1" 
                       FontSize="200"/>
        </StackPanel>
    </Grid>
</Page>
